# 扩展深度测试 - 对比分析

## 🎯 测试目标

将射线追踪范围从 32mm (3.2cm) 扩展到 256mm (25.6cm)，以覆盖所有Bragg峰深度：
- 120 MeV: 103.75 mm ✓
- 160 MeV: 173.92 mm ✓
- 180 MeV: 214.05 mm ✓

---

## 📊 参数对比

| 参数 | 原始测试 | 扩展测试 | 覆盖范围 |
|------|--------|--------|--------|
| **z维度** | 32 | 256 | 3.2cm → 25.6cm |
| **z分辨率** | 1mm | 1mm | (保持相同) |
| **射线步数** | 100 | 256 | 扩展追踪 |
| **内存占用** | 基准 | ~8倍 | CT + 剂量 |

### 关键几何参数

```
[GEOM] gantry Z bounds (mm):
  原始: z=[-32, 0], steps=33
  扩展: z=[-256, 0], steps=257
```

---

## 🔬 关键发现

### 1. 射线权重的剂量单位变化！

**原始测试:**
```
[RAY_W] Layer 0 sum=9.959792e-01  ← 相对权重，范围[0,1]
```

**扩展测试:**
```
[RAY_W] Layer 0 sum=9.959792e+07  ← 绝对剂量值！单位可能是10^-2 Gy
```

**差异系数:** 10^8 倍！这表明当z维度增加时，剂量计算的单位标度发生了变化。

### 2. IDD (能量沉积)的变化

**原始测试:**
```
Ray IDD - sumFinite=4.735916e-06  max=9.335524e-11
```

**扩展测试:**
```
Ray IDD - sumFinite=3.051784e+03  max=1.492304e-02
```

**差异系数:** 10^9 倍！这是射线追踪深度增加导致的自然结果。

### 3. BEV剂量的变化

**原始测试 (z=1-5):**
```
z=1 sum=3.067029e-07  [浅层峰值]
z=2 sum=1.534471e-07
```

**扩展测试 (z=1-5):**
```
z=1 sum=3.068513e+01  [浅层峰值]
z=2 sum=1.535213e+01
```

**比率:** 仍然是约 **2:1**（z=1:z=2），说明浅层分布是一致的！

### 4. 重要！超过z=5后仍然为零

```
扩展测试z分布:
  z=0: 0
  z=1: 3.069e+01 [PEAK]
  z=2: 1.535e+01
  z=3: 1.536e+01
  z=4: 1.534e+01
  z=5: 1.536e+01
  z=6-255: 全部 0.0 ！
```

**这说明:**
- ❌ 即使扩展到256mm，仍然看不到 Bragg峰！
- ❌ z>5后的剂量仍然硬截断为零
- ❌ 问题**不在体积深度**，而在射线追踪或计算本身

---

## ⚠️ 关键观察：问题所在

### 问题1: z>5硬截断不是因为体积深度

原始测试体积深度: 3.2cm
扩展测试体积深度: 25.6cm (增加8倍)

但结果: **z>5仍然为零！**

### 问题2: 射线追踪可能被限制

```
[CUTOFF] 信息（截断设置）:
  maxPeakDepth_mm = 214.05 (180 MeV的Bragg峰)
  firstPastCutoffAll = 256 (步数)
```

这表明：
- 代码知道最大Bragg深度 (214mm)
- 但追踪步数被设置为 256 (=z维度)
- **可能的原因：射线在到达Bragg峰之前就停止了**

### 问题3: 缩放因子的变化

观察到 `[RAY_W]` 的单位从 e-01 变为 e+07，说明：
- 可能有内部缩放因子的变化
- 或者单位的定义发生了变化
- 需要检查是否有"剂量至相对权重"的变换

---

## 📈 深度分析：为什么看不到Bragg峰？

### 假设1: 射线被厚水层完全吸收

```
射线射程 vs 追踪范围:
  120 MeV质子: 
    - 总射程: 105 mm
    - 我们追踪: 256 mm ✓ (足够)
  160 MeV质子:
    - 总射程: 175 mm
    - 我们追踪: 256 mm ✓ (足够)
  180 MeV质子:
    - 总射程: 215 mm
    - 我们追踪: 256 mm ✓ (足够)

结论: 深度范围应该足够！
```

### 假设2: 创建CT数据时有边界

查看 `createTestCTData` 函数：

```cuda
// 创建球形幻象模型
if (dist < radius) {
    data[idx] = huDist(generator);  // 组织
} else {
    data[idx] = 50.0f + huDist(generator) * 0.1f;  // 空气
}
```

**问题:**
- 球形幻象的 `radius = dims.z / 3 = 256 / 3 ≈ 85` 层
- 中心 `centerZ = dims.z / 2 = 128` 层
- **最大深度: z=128+85=213 ~ Bragg峰位置！**
- **z>213后: 全是空气！**

这可能导致：
- 射线在z>213处无法继续追踪（全是空气）
- 实际追踪范围被CT几何限制了！

### 假设3: 追踪被 `beamFirstPassive` 截断

```
  beamFirstPassive: 256 (所有射线在step 256后都是passive)
  afterLast: 256
```

**这说明:**
- 所有射线在第256步都停止计算
- 即使体积更深，追踪也被限制了

---

## 🔧 解决方案

### 短期修复: 扩展CT幻象的深度

修改 `createTestCTData()`:

```cuda
// 原始：
int radius = std::min({dims.x, dims.y, dims.z}) / 3;

// 改为：
int radius = std::min({dims.x, dims.y}) / 3;  // 仅限制XY，Z方向全部是组织
```

或更好的方案：

```cuda
// 创建完全均匀的水层（而不是球形）
for (int z = 0; z < dims.z; ++z) {
    for (int y = 0; y < dims.y; ++y) {
        for (int x = 0; x < dims.x; ++x) {
            int idx = z * dims.x * dims.y + y * dims.x + x;
            // 整个体积都是水（类似人体组织）
            data[idx] = 1000.0f + huDist(generator);  // 水的HU = 0
        }
    }
}
```

### 中期修复: 增加射线步数

当前 `beam.steps = 256` 对应 256 steps。

对于280mm的完整追踪，应该是 `beam.steps = 280` 或更多。

### 长期改进: 分层CT（水+组织+骨）

```cuda
// 创建更现实的分层模型
for (int z = 0; z < dims.z; ++z) {
    float hu;
    if (z < 100) {
        hu = 1000.0f;  // 水层 (z=0-100mm)
    } else if (z < 200) {
        hu = 40.0f;    // 软组织 (z=100-200mm)
    } else {
        hu = -1000.0f; // 空气或完全穿透 (z>200mm)
    }
    // ... 填充数据
}
```

---

## 📋 建议的验证步骤

### Step 1: 确认CT几何限制 (立即做)

```bash
# 修改createTestCTData，输出Z方向的组织分布
grep -A 20 "createTestCTData" src/tests/wrapper_integration_test.cu
```

预期：球形幻象在z~213处达到最大深度

### Step 2: 创建完整水层测试 (10分钟)

```cuda
// 在wrapper_integration_test.cu中新增：
std::vector<float> createWaterPhantom(int3 dims) {
    std::vector<float> data(dims.x * dims.y * dims.z, 1000.0f);
    // 全部填充为1000 (纯水)
    return data;
}

// 然后修改main():
std::vector<float> imVolData = createWaterPhantom(imVolDims);  // 用水代替球形
```

运行后，应该能看到Bragg峰！

### Step 3: 分析Bragg峰位置 (5分钟)

运行新测试，使用 `analyze_deep_decay.py` 检查z分布：

```bash
RTD_TEST_USE_REFERENCE_LUT=1 ./bin/wrapper_integration_test > water_phantom_test.log
python3 analyze_deep_decay.py water_phantom_test.log | grep -A 50 "COMPLETE Z-SLICE"
```

应该看到：
```
z=0-100: 浅层
z=100-103: Bragg峰前升高
z=103-107: Bragg峰（120 MeV）
z=107-175: 中间衰减区
z=175-180: Bragg峰2（160 MeV）
z=180-214: Bragg峰3（180 MeV）
z>214: 快速下降
```

---

## 🎓 结论

| 项目 | 发现 |
|------|------|
| **体积深度** | ✓ 已扩展到 256mm (25.6cm) |
| **射线步数** | ✓ 已增加到 256 步 |
| **看到Bragg峰？** | ✗ 仍然没有！ |
| **根本原因** | CT幻象体（球形）的边界限制 |
| **快速修复** | 使用水层而不是球形幻象 |
| **预期改进** | 应能看到完整的Bragg峰特征 |

---

## 📌 总结表

```
┌─────────────────────────────────────────────────────────┐
│ 原始测试 vs 扩展测试                                     │
├──────────────────┬─────────┬─────────┬──────────────────┤
│ 参数             │ 原始    │ 扩展    │ 差异             │
├──────────────────┼─────────┼─────────┼──────────────────┤
│ z维度            │ 32      │ 256     │ 8倍增加          │
│ 体积深度         │ 3.2cm   │ 25.6cm  │ 追踪深度扩展 ✓  │
│ 射线步数         │ 100     │ 256     │ 步数增加 ✓       │
│ z=1剂量          │ 3.1e-7  │ 3.1e+1  │ 10^8倍缩放变化   │
│ z=1:z=2比率      │ 2.00    │ 2.00    │ 一致 ✓           │
│ z>5剂量          │ 0       │ 0       │ 仍为零 ✗         │
│ Bragg峰可见      │ ✗       │ ✗       │ CT几何限制       │
└──────────────────┴─────────┴─────────┴──────────────────┘
```

---

## 🎯 下一步行动

优先级：

1. **P1 (立即)**: 创建水层幻象代替球形幻象，重新运行测试
2. **P2 (可选)**: 分析Bragg峰的实际位置和形状
3. **P3 (优化)**: 创建更现实的分层CT模型（皮肤、脂肪、骨等）

