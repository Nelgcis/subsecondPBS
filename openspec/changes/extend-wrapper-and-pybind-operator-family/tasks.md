Current priority override: Section 9 halo / nucleus-correction migration audit is now the highest-priority thread for this change line. Do not implement runtime halo blindly: first lock the compile-time macro contract, the runtime gating contract, and the upstream stage map. Section 0 final-dose correctness still remains the numeric enablement gate before halo can be trusted in TPS.

Halo dose-deficit root-cause sequencing (added 2026-05-08, see `halo-energy-conservation.md`):
The dominant mechanism for "halo-on dose lower than primary-only" is the `nucIdxToFan` lenToMm doubling at `raytracedicom_wrapper.cu` ~5132-5141 (RC2). Lattice-grid identity (RC1/RC3/RC4) only becomes observable in lateral profile after RC2 lands. Therefore the staged order is **9.48 first** (one-line BEV-positioning fix), then 9.49 audit gate, then 9.21-9.40 implementation in the order specified in 9.52. Do not start 9.23 / 9.34 implementation until 9.48 is green on the fixture and 9.51 has frozen gate-A/B baselines.
Current status note: imported `water_equivalence` sampling has been restored to direct tracer-step (`k -> WEQ[k]`) semantics after a projected-depth remap introduced a large depth-axis shift and regressed wrapper dose output. Tasks `0.4`, `0.5`, and `2.3` are now implemented in the wrapper test and wrapper audit code, pending CUDA runtime numeric verification.
Current halo note: upstream RayTraceDicom drives halo through compile-time `NUCLEAR_CORR` variants (`SOUKUP`, `FLUKA`, `GAUSS_FIT`) plus a separate nuclear tracing / superposition / transfer branch. The local root `CMakeLists.txt` now exposes the same macro family, pybind and wrapper still accept `nuclear_correction`, and internal `EnergyStruct` loading still knows the nuclear LUTs under `#ifdef NUCLEAR_CORR`. But the public wrapper contract and live runtime are still not halo-complete: `RTDEnergyStruct` remains proton-only, pybind only fills density/SP/RRL through the public path, and the live wrapper still runs proton `fillIddAndSigma -> primary superposition -> primTransfDiv` only.
Current halo migration note: treat Section 9 as the active migration and audit plan, not as a landed runtime status report. Older notes that described a compiled or verified local halo runtime chain should be considered stale. The next real runtime blockers are the public nuclear energy-data contract, the local halo lattice plan derived from `RawSpotLattice`, the dual-output `fillIddAndSigma` surface, the separate nuclear superposition path, and `nucTransfDiv`.
Current RTD-main migration audit note (2026-04-29): a fresh side-by-side read of `patch10_mod_20260410/include` + `src` against local `RayTraceDicom-main (1)/RayTraceDicom-main/src` confirms the migration is still incomplete under the strict "no omission / no hard-coded fallback" rule. The highest-impact gaps are: (a) runtime `nuclear_correction=true` is still rejected in `subsecondWrapper`; (b) `iddParams.rRlScale` is still hard-coded to `1.0f` instead of using table-driven radiation-length scaling; (c) CPB/ray geometry still contains fallback constants (`doseResolution * 0.5f`, full-dose-box fallback coverage) that are not upstream-equivalent primary-path semantics.
Current dose-grid contract note (2026-04-20): Python-side normalization in `tps_py/dosecal.py`, pybind-side ROI parsing in `src/bindings/raytracedicom_pybind.cpp`, and wrapper-side `computeDoseRoiBounds(...)` now all reject stale or out-of-range ROI linear indices against the active `doseGrid.dims` instead of warning or silently skipping them. This is a narrow Section 0 / 3.3 prerequisite only: it hardens the shared ROI-vs-doseGrid contract without closing `0.4`, `2.3`, or `3.3` until CUDA fixture runs confirm the end-to-end geometry path on the live wrapper.
Current RTD module import note (2026-04-21): the pybind module init symbol now matches the produced `cudaCalDoseRTD` filename. Local rebuilds can therefore import the RTD final-dose module directly for both proton-only and halo-enabled build variants instead of depending on a filename / `PyInit_*` mismatch workaround.

## 0. Dose Correctness First

- [x] 0.1 Freeze one comparison vocabulary for all follow-up work: distinguish patient `x`, physical `x(mm)`, beam depth along the `-x` propagation direction, RTD fan `z`, and array index order so future debugging and plots never mix “index x” with “depth”.
- [x] 0.2 Make the dose comparison procedure beam-aware. For every wrapper-vs-CarbonPBS check, compare in physical coordinates and beam-depth coordinates, not raw array indices, and document which side of the grid is proximal versus distal for this beam.
- [x] 0.3 Prove the depth-axis contract between the RTD fan start plane and the exported `water_equivalence` header before touching more API-surface work. Compare `sourceDistVec - fanCorner_mm.z` against `weqHeader[0]`, verify whether `fillBevFromWeqVolumeKernel` is currently ignoring a non-zero projected-start offset, and lock the sign/offset convention that keeps RTD depth aligned with CarbonPBS when physical depth runs along patient `-x`.
- [x] 0.4 Audit `water_equivalence` step/count alignment end-to-end. Check `weqHeader[1]`, `weqHeader[2]`, `tracerSteps`, `beamFirstInsideRT`, `beamFirstOutsideRT`, `firstPositive/lastPositive`, and IDD `scaleFacts` together so proximal-tail inflation and distal premature cutoff can be traced to one concrete sampling contract instead of being mistaken for a viewer or reshape issue.
- [x] 0.5 Validate the `beamFirstInsideRT -> entryZ_mm -> pxSpMult -> entrySigma` chain against CarbonPBS expectations so the BEV entry plane used by convolution is not depth-shifted and does not create artificial lateral layering or y-direction jaggedness.
- [x] 0.6 Audit raw spot decoding and lattice phase preservation. Verify that `idbeamxy` texel-center recovery, dense-plane rasterization, and ray-grid alignment preserve CarbonPBS spot centroids and do not introduce alternating-layer offsets.
- [x] 0.7 Audit the raw-spot-lattice CPB/ray crop against the actual fan support required by the projected dose volume. The current `hasRawSpotLattice` branch crops from spot bounds plus sigma margin and ignores the larger projected dose-box bounds; verify whether this truncates valid lateral support and produces rectangular zero regions or y-direction sawtooth artifacts.
- [x] 0.8 Probe the BEV-to-dose writeback box and inverse transform on the current beam. Capture `primMinPoint/primMaxPoint`, `startIdx/maxIdx`, and representative `doseIdx -> bevIdx` mappings near the abnormal regions, then verify that the beam-aligned distal tail is not being clipped by the transfer box before the BEV dose naturally decays.
- [x] 0.9 Replace the current `[GEOM] Ray grid does not overlap CT volume (XY)` warning with a beam-aware support audit that understands beams almost parallel to patient `x`, so the investigation follows true support/crop errors instead of a misleading entry-plane heuristic.
- [ ] 0.10 Add beam-aligned regression diagnostics for the direct-wrapper path: support masks, 1D profiles along beam depth and transverse directions, distal falloff width, proximal tail integral, and slice-by-slice continuity metrics.
- [ ] 0.11 Close the final-dose geometry issue only when wrapper output is simultaneously correct in support extent, proximal tail, distal tail, and XY smoothness relative to CarbonPBS or the accepted reference fixture.
- [ ] 0.12 Only after Section 0.1 through 0.11 are stable, revisit CarbonPBS-only profile/subspot corrections as second-order smoothing work; do not use them to mask primary depth, crop, or transfer-box errors.

## 1. Change Setup

- [x] 1.1 Create the OpenSpec change for the combined direct-wrapper and `.so` operator-family work, and document that the primary first milestone is direct final-dose from `test_data` in `wrapper_integration_test`.
- [x] 1.2 Lock one explicit variable contract for `test_data` that maps the exported values back to `doseCal.py -> cuCalDose/cuFinalDose` inputs, including which fields are required for direct final dose, which are required for operator outputs, and which are required for biological outputs.

## 2. Direct Wrapper Final-Dose Path

- [x] 2.1 Audit `wrapper_integration_test` loaders so they consume the current `test_data` naming and metadata as the canonical direct-wrapper final-dose path, without relying on pybind-only preparation.
- [x] 2.2 Add direct-wrapper contract checks for key arrays and metadata, including `water_equivalence`, `idbeamxy`, `layer_info`, `layer_energy`, `beam_dir`, `subspot_data`, `beam_para_data`, and dose-grid metadata.
- [x] 2.3 Verify that the direct wrapper test writes a `dosegrid` from `test_data` through the RTD wrapper path and compare that output against the existing reference-dose checks.

## 3. Shared Final-Dose Normalization

- [x] 3.1 Refactor the pybind-side CarbonPBS normalization so final-dose pybind entries and direct-wrapper test preparation follow the same contract meanings, even if they use different loader code.
- [x] 3.2 Keep `cuCalDose` and `cuFinalDose` on the same RTD wrapper-backed final-dose path as the direct-wrapper execution route.
- [ ] 3.3 After Section 0 is resolved, factor the corrected geometry/sampling/crop contract into shared helpers so wrapper execution and pybind final-dose entries cannot drift in depth mapping, ray support, or dose writeback behavior.
- [ ] 3.4 Freeze one corrected final-dose regression baseline and require every later migration step to preserve that baseline before operator-family work is considered ready.

## 4. Honest `.so` Operator-Family Surface

- [x] 4.1 Remove or replace the current fake RTD `cuCalDose3` behavior so the `.so` no longer silently returns zero CSC outputs as if that were valid compatibility.
- [x] 4.2 Decide and document the interim `.so` behavior for unimplemented operator-family functions, using either explicit runtime failure or explicit capability-gated support rather than silent substitutes.
- [x] 4.3 Add the `.so` API surface plan for the required function family, covering `cuCalDose`, `cuFinalDose`, `cuCalDoseNorm`, `cuCalDose3`, `cuFinalDoseAndRBEMap`, and any explicitly deferred biological helper functions.
- [ ] 4.4 Publish a function-by-function migration matrix mapping every imported CarbonPBS entry used by `doseCal.py` to one RTD mode: corrected final-dose mode, future spot-operator mode, direct biological mode, or explicit deferment.
- [ ] 4.5 Update backend-selection planning so `doseCal.py` can switch by explicit capability after the corrected final-dose baseline is stable, rather than by mixed historical imports.

## 5. Spot-Preserving Operator Mode

- [ ] 5.1 Introduce a shared internal RTD operator-mode design that preserves spot identity through batched processing instead of reusing the already-aggregated final-dose intermediate.
- [ ] 5.2 Implement the internal row-norm accumulation path needed by `cuCalDoseNorm`, using spot-preserving batched RTD coefficients and ROI-restricted accumulation.
- [ ] 5.3 Add validation for `cuCalDoseNorm` using small explicit cases and cross-checks against CSC-derived row norms or CarbonPBS reference behavior.
- [ ] 5.4 Implement the internal two-pass CSC emission path needed by `cuCalDose3`, including count-first then write-second behavior and preservation of the legacy `out_cscptr` count semantics expected by Python-side postprocessing.
- [ ] 5.5 Add validation for `cuCalDose3` by checking CSC structure, ROI row indices, and the consistency of `A * w` against RTD final-dose output on controlled cases.
- [ ] 5.6 Reuse the corrected final-dose geometry, WEQ mapping, and transport chain inside operator mode; do not create a second independent geometry path for CSC or norm generation.
- [ ] 5.7 Add a spot-batch memory and kernel-shape plan that keeps RTD’s CUDA/superposition efficiency intact while preserving per-spot identity for CSC and norm outputs.
- [ ] 5.8 Validate operator outputs against the corrected final-dose baseline, not the pre-fix wrapper behavior, by checking that `A * w` reproduces the repaired RTD dose grid on controlled fixtures.

## 6. Biological Map Path

- [ ] 6.1 Design the direct RTD biological final-map path for `cuFinalDoseAndRBEMap` so it shares the same normalization and physical chain as final dose instead of depending on CSC export.
- [ ] 6.2 Decide which CSC-dependent biological helper functions remain deferred until the operator path is fully real, and document any explicit deferment in the change artifacts.
- [ ] 6.3 Gate biological-map migration on the corrected final-dose geometry contract so RBE or norm-related outputs cannot inherit the same depth/crop bugs under a new name.

## 7. Verification And Routing

- [ ] 7.1 Add tests that cover both the direct-wrapper final-dose path and the `.so` operator-family surface, making clear which functions are production-ready, which are staged next, and which are explicitly unsupported.
- [ ] 7.2 Update the final design notes and task status with a function-by-function support matrix so future `doseCal.py` backend selection can switch from mixed CarbonPBS/RTD behavior to explicit capability-based routing.
- [ ] 7.3 Organize verification in two gates: first a final-dose correctness gate for wrapper/pybind shared geometry, then an operator-family capability gate for CSC, norm, and biological outputs.

## 8. Runtime Performance Follow-Up

Section 8 is a planning and follow-up record for the current `openspec-explore` performance audit. Do not let performance work outrun unresolved halo migration work from Section 9 or unresolved final-dose correctness findings from Section 0. The only exceptions that may run earlier are measurement-stability and resource-lifetime tasks such as baseline profiling and confirmed texture-lifetime fixes.

- [ ] 8.1 Record a fixture-backed performance baseline for the wrapper-backed path, including total wall time plus per-stage timings for pybind marshalling, texture creation, beam setup, WEQ or ray tracing, convolution, IDD or sigma, superposition, BEV-to-dose transfer, and final writeback. Also count the current explicit `cudaDeviceSynchronize()` sites and major alloc/copy sites so later optimizations can be compared against one stable baseline.
- [ ] 8.2 Fix texture resource lifetime before deeper optimization. `src/utils/utils.cu` currently allocates backing `cudaArray` storage inside `create1DTexture(...)`, `create2DTexture(...)`, and `create3DTexture(...)`, while the wrapper teardown only destroys the texture objects. Introduce an explicit texture-handle ownership model or RAII wrapper so `imVolTex`, LUT textures, `profileTex`, and `bevPrimDoseTex` release both object and array cleanly across repeated TPS calls.
- [ ] 8.3 Compare the current wrapper against `RayTraceDicom-main (1)/RayTraceDicom-main/src/kernel_wrapper.cu` and remove wrapper-only full-device fences that are not algorithmically required. The current wrapper path has many explicit `cudaDeviceSynchronize()` calls, while the upstream kernel wrapper largely relies on kernel ordering, memcpy edges, and optional event timing instead of global device stalls.
- [ ] 8.4 Thread one explicit non-blocking CUDA stream through the wrapper runtime and helper functions so kernel launches, copies, and frees can use stream semantics instead of the current default-stream plus global-sync behavior.
- [ ] 8.5 Introduce CUDA 12.1 stream-ordered allocation for short-lived workspace buffers with `cudaMallocAsync` / `cudaFreeAsync` or `cudaMallocFromPoolAsync`, and keep the mempool configured for reuse instead of immediate release. Do not drop in `src/utils/advanced_memory_pool.cu` unchanged until its zero release-threshold setting and stream model are audited.
- [ ] 8.6 Replace repeated per-beam and per-layer allocation churn with reusable workspaces sized to the current maximum beam or layer shape. Candidate buffers include `devRayWeightsAllLayers`, BEV tracing buffers, raw-spot convolution planes, per-layer IDD or sigma buffers, padded superposition inputs, BEV dose buffers, and temporary scalar-reduction storage.
- [ ] 8.7 Remove avoidable host round-trips from beam and layer reductions. Consolidate `beamFirstInside`, `beamFirstOutside`, `weplMin`, `beamFirstPassive`, and related cutoff bookkeeping so only the minimal scalar state required by control flow crosses back to host, and only once per beam or layer.
- [ ] 8.8 Audit `performCompleteTileBasedSuperposition(...)` and related helpers for hidden internal sync/copy costs. Decide whether tile-radius bucketing, overflow handling, and counter updates can remain numerically identical while moving more of the bookkeeping onto device.
- [ ] 8.9 Make async copies real, not cosmetic. Identify which pybind/TPS buffers are pageable NumPy or host memory, then introduce pinned staging or registration for large CT, WEQ, and dose transfers before converting wrapper copies to `cudaMemcpyAsync`.
- [ ] 8.10 Reduce pybind marshalling overhead before the wrapper launch by eliminating unnecessary NumPy-to-`std::vector` copies where inputs are already contiguous and contract-safe, especially for `weq_data`, IDD/LUT tables, profile data, beam-parameter data, and subspot data.
- [ ] 8.11 Remove or narrow compatibility-only host writeback overhead. Explicitly measure the `commit_output_dose(...)` `(Nz,Ny,Nx) -> (Nx,Ny,Nz)` triple-loop path and decide whether the public compatibility contract should prefer native layout or a cheaper conversion strategy.
- [ ] 8.12 Audit and salvage existing experimental infrastructure safely. Reuse ideas from `src/utils/advanced_memory_pool.cu`, `src/utils/texture_pool.cu`, and `src/utils/precompiled_texture_manager.cu` only after validating correctness risks such as zero mempool release threshold, content-agnostic texture cache keys, and device-side staging misuse.
- [ ] 8.13 Evaluate CUDA Graph capture only after stream ownership, buffer reuse, and synchronization cleanup are stable. Target the steady-state TPS case where beam counts and major tensor shapes repeat, and use graph upload/update only if the captured topology remains fixed.
- [ ] 8.14 Add a performance regression gate to the direct-wrapper and pybind fixture workflows so future numeric fixes do not silently reintroduce allocation churn, texture leaks, default-stream serialization, or extra global synchronizations.

## 9. Halo / Nuclear Correction Migration

Section 9 captures the current `openspec-explore` audit for bringing back the RayTraceDicom halo path. Upstream RTD treats halo as a macro-gated second physical branch, not as a scalar tweak on the proton branch. Preserve that mental model while auditing this repo: proton-only execution must stay valid when the macro path is off, and halo-on execution must add the missing nuclear branch without changing the existing dose-grid layout contract by accident.

Current migration snapshot:

- Build and public contract:
  root `CMakeLists.txt` now mirrors upstream `NUCLEAR_CORR` options, but public `RTDEnergyStruct` is still proton-only and pybind public loaders still do not carry nuclear halo tables through the wrapper contract.
- Live runtime truth:
  local wrapper is still proton-only at runtime: `fillIddAndSigmaKernel`, superposition, and BEV-to-dose transfer all run on the primary branch only. `RawSpotLattice` is already live and used for local spot-phase / ray-grid geometry, but no live `devNuc*`, dual-output IDD, nuclear superposition, or `nucTransfDiv` path exists yet. `nucMemStep` is currently an inert placeholder, not evidence of a working halo branch.
- Primary-safe insertion strategy:
  local wrapper is layer-local, not beam-global. The lowest-risk migration path is therefore to keep the existing proton path intact and add a per-layer nuclear sibling path: `RawSpotLattice.spotWeights -> padded nuclear weights -> devNucSpotIdx -> dual-output IDD -> nuclear superposition -> nucTransfDiv -> same doseVol`.
- File/function map:
  build gate in `CMakeLists.txt`; public energy contract in `include/core/raytracedicom_integration.h`; internal nuclear LUT loading in `include/utils/energy_struct.h` plus `src/utils/energy_reader.cpp`; public nuclear LUT carriage in pybind and fixture loaders; dual proton+nuclear IDD in `include/algorithms/idd_sigma.h` plus `src/algorithms/idd_sigma.cu`; nuclear superposition sibling beside `performCompleteTileBasedSuperposition(...)`; nuclear transfer sibling beside `primTransfDiv`; wrapper integration and buffer orchestration in `src/core/raytracedicom_wrapper.cu`.

- [x] 9.1 Finish locking the build-time and runtime gating contract before writing runtime halo code. The local build system already mirrors the upstream `NUCLEAR_CORR` option family (`SOUKUP`, `FLUKA`, `GAUSS_FIT`, `OFF`), but the pybind / wrapper `nuclear_correction` runtime contract is still incomplete while the live wrapper remains proton-only. Invalid combinations such as runtime `true` with no compiled nuclear branch, or runtime `true` on a proton-only public energy contract, must fail loudly instead of silently falling back.
- [x] 9.2 Freeze one upstream stage map for the halo chain, using `RayTraceDicom-main (1)/RayTraceDicom-main/src/kernel_wrapper.cu` and `.cuh` as the reference. The migration map must explicitly cover nuclear LUT texture binding, nuclear PB index mapping, dual-output `fillIddAndSigma(...)`, nuclear tile-radius bucketing, separate `kernelSuperposition<r>` launches, BEV nuclear dose texture creation, `nucTransfDiv`, and cleanup.
- [x] 9.3 Record the current local divergence table in source terms so implementation does not guess. Today the local tree has the root-level `NUCLEAR_CORR` build option, but `include/algorithms/idd_sigma.h` and `src/algorithms/idd_sigma.cu` still expose only the proton kernel signature, `src/algorithms/complete_superposition.cu` only handles the primary branch, `include/core/raytracedicom_integration.h` still exposes a proton-only `RTDEnergyStruct`, and `src/core/raytracedicom_wrapper.cu` has no live `devNuc*`, `bevNuc*`, or `nucTransfDiv` runtime path even though it still accepts `bool nuclearCorrection`.
- [x] 9.4 Verify that the local data and geometry contract is sufficient for halo before patching kernels. Confirm that `include/utils/energy_struct.h` and `src/utils/energy_reader.cpp` still load the required nuclear LUTs under `#ifdef NUCLEAR_CORR`, that the current beam / raw-spot-lattice / dose-grid metadata can reproduce the upstream `nucSpotIdx` and nuclear transfer geometry without inventing new TPS-side inputs, and record explicitly that the missing piece is the public wrapper contract rather than TPS-side source data.
- [x] 9.5 Decide whether the first migration step should restore the upstream dual-branch structure almost verbatim instead of forcing halo into the current simplified helpers. The default assumption should be “yes”: keep proton and nuclear workspaces separate until correctness is proven, because the current wrapper helpers were intentionally narrowed to a primary-only path.
- [x] 9.6 Design the local `fillIddAndSigma` API around two valid signatures: proton-only when `NUCLEAR_CORR` is off, and proton-plus-nuclear outputs when it is on. Do not thread dormant nuclear buffers through proton-only builds, and do not leave `nucMemStep` as a dead field that suggests support which does not actually exist.
- [x] 9.7 Reconstruct the nuclear PB lattice and padding rules. Upstream uses spot-grid dimensions for the nuclear branch, pads them with `extendAndPadd(...)`, and maps computational PB rays to nuclear PB indices through `devNucSpotIdx`; in the local tree that reconstruction should start from `RawSpotLattice` and its already-live spot-phase / ray-grid alignment rather than from an abstract beam-global spot-grid placeholder.
- [x] 9.8 Restore a separate nuclear superposition path instead of reusing the primary buffers in place. That means dedicated nuclear IDD and sigma buffers, nuclear tile counters, nuclear in-out index tables, and a separate BEV nuclear dose volume that can be batched by radius exactly as upstream does.
- [x] 9.9 Restore the final nuclear BEV-to-dose accumulation explicitly with the upstream geometry semantics. The current wrapper only builds `primRayIdxToDoseIdx` and launches the primary transfer path; halo migration must add the spot-grid-based `nucRayIdxToDoseIdx` and `nucTransfDiv` path and verify that it adds into the same dose box without introducing a new X/Z layout mismatch.
- [ ] 9.10a Prove the stage boundary between shared primary work and halo-only work on the live local runtime. Document explicitly that halo must not alter traced density / WEPL, `beamFirstInside/Outside/Passive`, primary ray weighting, or primary transfer geometry when `nuclear_correction=false`, and that the first halo-only branch begins at the nuclear outputs of the dual-output `fillIddAndSigma(...)` stage.
- [x] 9.10b Classify the remaining halo risks by effect, not just by file. Treat `HaloLatticePlan` / `devNucSpotIdx` and `nucRayIdxToDoseIdx` / `nucTransfDiv` as geometry-support risks that can change centering or support extent, while nuclear IDD deposition and nuclear superposition remain dose-physics risks that primarily change halo magnitude and lateral tails.
- [x] 9.10c Freeze the halo mismatch triage order before more runtime edits. If halo-on differs from upstream RTD or the accepted fixture, investigate in this order: `HaloLatticePlan` equivalence first, nuclear transfer geometry second, and only then consider replacing the local superposition launch wrapper with a more literal upstream call site.
- [x] 9.11a Define the halo gate matrix explicitly around three runs of the same controlled fixture: gate A = `NUCLEAR_CORR=OFF`; gate B = `NUCLEAR_CORR` build with `nuclear_correction=false`; gate C = `NUCLEAR_CORR` build with `nuclear_correction=true`.
- [x] 9.11b Use `src/tests/wrapper_integration_test.cu` as the single halo audit surface. Require it to report proton-vs-off-build drift for gate A/B and halo-vs-reference drift for gate C, instead of treating the current console output as informational only.
- [ ] 9.11c Do not enable halo in TPS until gates A and B prove proton invariance and gate C matches an accepted controlled case in total dose, support extent, and lateral-tail behavior. Halo-on agreement is not a valid signal if A/B are still open.
- [ ] 9.12a Quantify halo-extra workspace on the current layer-local path, using explicit audit hooks to separate primary bytes from halo-only bytes for IDD, sigma, BEV dose, and transfer support.
- [ ] 9.12b Quantify halo-extra support and transfer-box growth relative to the primary branch, because transfer coverage inflation may matter as much as raw buffer size for later performance work.
- [ ] 9.12c Feed the measured halo memory and transfer costs back into Section 8 priorities only after 9.11 is green, so performance follow-up reflects the actual accepted halo path instead of a transient debug configuration.
- [x] 9.13 Repair the public energy-data contract before enabling halo. `include/core/raytracedicom_integration.h` still exposes a proton-only `RTDEnergyStruct`, while `include/utils/energy_struct.h` and `src/utils/energy_reader.cpp` still know the nuclear LUTs. Decide whether to extend `RTDEnergyStruct` with optional nuclear matrices or introduce an internal bridge type, then update pybind and fixture loaders accordingly.
- [x] 9.14 Do not assume the upstream nuclear LUT depth grid matches the current CarbonPBS-backed `RTDEnergyStruct` depth grid. The local pybind path can build `ciddMatrix`, `peakDepths`, and `scaleFacts` from imported CarbonPBS IDD tables, while upstream `nuclear_weights_and_sigmas_fit.txt` is tied to the reference RTD proton table. Define and implement whether halo mode uses the reference RTD energy table wholesale or resamples the nuclear LUTs onto the active local energy/depth axis; otherwise the halo branch can silently mix incompatible tables.
- [x] 9.15 Prefer a layer-local halo migration first, not a beam-level wrapper refactor. The current local wrapper already does per-layer `fillIddAndSigma -> superposition -> transfer -> dose accumulation`, and those transfer steps are linear. Exploit that to add a per-layer nuclear superposition plus `nucTransfDiv` path beside the existing primary path, then only consider a later beam-level consolidation if performance or exact upstream structure requires it.
- [x] 9.16 Lock the first-version nuclear weight source explicitly. The safest upstream-faithful default is to derive nuclear PB weights from `rawSpotLattice.spotWeights` on the canonical CarbonPBS spot plane, padded to tile multiples and mapped through `nucSpotIdx`, while leaving the current subspot-aware primary convolution unchanged. Record whether subspot-aware halo centroids are deferred or required for parity on production plans.
- [x] 9.17 Remove the current hard runtime rejection for `nuclear_correction=true` in `subsecondWrapper` and replace it with the real macro-gated branch behavior. Runtime `true` must either execute the compiled `NUCLEAR_CORR` branch or fail only for explicit contract violations (missing/alignment-invalid nuclear LUT payload), not for a blanket "not wired yet" guard.
- [x] 9.18 Replace hard-coded `iddParams.rRlScale = 1.0f` with the RTD-main-equivalent radiation-length scaling contract. The source of truth must be the active energy-data scaling payload (upstream `iddData.rRlScaleFact` semantics), and halo/proton builds must share the same scaling meaning.
- [x] 9.19 Eliminate primary-path geometry fallback constants that are not part of the upstream algorithm contract. Specifically audit and either remove or strictly capability-gate: CPB resolution fallback to `doseResolution * 0.5`, and projected-coverage fallback to full dose-box extents when upstream-equivalent spot/grid inputs are absent.
- [ ] 9.20 Add an explicit "no hard-coded physics constants in migrated primary path" audit gate in `wrapper_integration_test`/audit output. The gate should fail if migrated primary stages silently use fallback constants in place of upstream-derived inputs for radiation scaling, CPB spacing, or coverage support.
- [ ] 9.21 Lock one explicit lattice contract for halo before further runtime edits: primary CPB rays may be dense, but halo HPB must be derived from one stable physical PB grid per layer (upstream `beam.getSpotIdxToGantry()` semantics), not from per-layer non-zero-weight spot sparsity.
- [x] 9.22 Remove the current heuristic halo-lattice reconstruction in `buildHaloLatticePlan(...)` that uses `uniqX/uniqY`, average spacing, and positive-weight filtering to infer `spotDelta/spotOffset`.

  **Implementation Status (2026-05-09):** `buildHaloLatticePlan` already builds from the dense rasterized lattice; with 9.23 landed, the source of `RawSpotLattice` for halo is now the canonical PB grid view from `buildPhysicalPBLatticeView`, so no `uniqX/uniqY` reconstruction remains in the active halo path.
- [x] 9.23 Rebuild `HaloLatticePlan` around the upstream-equivalent source of truth: physical PB delta and offset from the canonical spot-index-to-gantry transform, with only tile divisibility padding (`roundTo(..., superpTileX/Y)`) applied afterward.

  **Implementation Status (2026-05-09):** `buildPhysicalPBLatticeView` rewritten in `src/core/raytracedicom_wrapper.cu` to infer `(nx, ny, dx, dy, ox, oy)` from decoded spot center coordinates (median consecutive diff + integer-multiple uniformity validation) instead of `weqHeader[8/5/7/4/6/3]`. Local equivalent of upstream `beam.getSpotIdxToGantry()`.

  **Relaxation pass (2026-05-09):** Initial 5% per-diff tolerance and `k <= 16` upper bound rejected a real fixture (`bg838_beam964`) at `ratio=0.888890`. Tolerances loosened to 25% per-diff and `k <= 32`. When canonical inference still fails, the function now falls back to `weqHeader[8/5/7/4/6/3]` (the pre-9.23 lattice) with a `[RTD_PB_GRID] WARN: ... falling back to WEQ-header lattice` message. RC2 BEV positioning fix remains active in fallback mode but RC3/RC4 lateral profile is NOT upstream-equivalent. Out-of-bound binning during fallback is soft-clamped to the nearest cell rather than aborting.
- [x] 9.24 Freeze and enforce the area-normalization precondition for `spotDist^2`: `iddParams.spotDist` must be computed as `physical_PB_spacing / CPB_spacing` (in rays), and must never be driven by runtime sparse occupancy statistics.

  **Implementation Status (2026-05-09):** Existing audit at lines 4019-4023 (which required `approxEq(haloDelta, primaryDelta)`) replaced with integer-multiple ratio check (`haloDelta = k * primaryDelta`, k ∈ [1, 32], tolerance 5%). Pre-9.23 contract (halo == primary) was upstream-incorrect; now correct.

  **Relaxation pass (2026-05-09):** Demoted from `throw std::runtime_error` to `[9.24] WARN` log when the integer-multiple check fails. Tolerance loosened from 5% to 25% to match `inferAxis`. Companion site-1/site-2 `approxEq(spotDist, expectedSpotDist)` checks similarly demoted to `[HALO_AUDIT] WARN` (warn-once-per-thread × 3) with tolerance loosened to 1e-3. Halo runs are no longer blocked by audit mismatches; warnings still surface real lattice issues for visibility.
- [ ] 9.25 Separate "missing spot weight" from "missing lattice point" in halo mapping: keep HPB lattice geometry complete and deterministic, while allowing zero weights to remain zero without collapsing lattice spacing.
- [ ] 9.26 Add a dedicated halo-geometry audit dump in `wrapper_integration_test` for one fixed fixture (`bg838_beam964` or equivalent): per layer report physical PB delta, CPB spacing, `spotDistInRays`, HPB padded dims, and non-padded physical dims; fail when these values drift from upstream RTD semantics.
- [ ] 9.27 Add an RTD-main parity check for nuclear index mapping: verify local `devNucSpotIdx` construction matches the upstream "project physical PB centre to CPB ray and write one fixed spot index" behavior, rather than nearest-from-unique-axis lookup.
- [ ] 9.28 Require gate-C halo validation to include central-field dose invariance checks after halo enablement. If center dose drops while tails rise, triage order must start from lattice/`spotDist` normalization mismatch before tuning nuclear LUTs or superposition radius logic.
- [ ] 9.29 Record and enforce the allowed coarse-halo condition explicitly: `halo spotDelta > 1` is valid only when it equals physical PB spacing relative to CPB spacing; "arbitrary sparse coarse grid" is an invalid model and should fail contract checks.
- [ ] 9.30 Stage implementation into four mergeable micro-steps to reduce regression risk: (A) freeze/print physical lattice contract, (B) replace `HaloLatticePlan` geometry source, (C) rewire `iddParams.spotDist` and `devNucSpotIdx` to the new contract, (D) rerun A/B/C gate matrix with numeric diff reports.
- [x] 9.31 Add an explicit source-of-truth rule to code and audit docs: halo lattice geometry must be derived from the canonical physical PB transform (`beam.spotDelta/spotOffset` or `rawSpotLattice.spotDelta/spotOffset` under validated equivalence), never from per-layer sparse occupancy statistics (`uniqX/uniqY` or nearest-axis reconstruction).

  **Implementation Status (2026-05-09):** Unit-contract comment block added above `buildHaloLatticePlan` and `buildPhysicalPBLatticeView` documenting that `spotDelta/spotOffset` are mm and must come from canonical decoded spot positions. 9.23 enforces this in code.
- [x] 9.32 Make `buildHaloLatticePlan(...)` RTD-main-equivalent in structure: compute raw HPB dimensions from the physical PB grid cardinality, then apply only tile padding (`SUPERP_TILE_X/Y`) before writing padded halo weights (`extendAndPadd` semantics), with no axis re-fit and no per-layer inferred spacing.

  **Implementation Status (2026-05-09):** With 9.23, `buildHaloLatticePlan` consumes `rawSpotLattice` whose `spotGridDims` is now the canonical PB grid (5×5 typical) instead of WEQ (80×60). Tile padding via `roundUpToMultiple(rawX, SUPERP_TILE_X)` produces the upstream-equivalent `nucRayDims = padded(spotGridDims)`.
- [x] 9.33 Mirror RTD-main `spotDistInRays` semantics exactly in the active halo path: `iddParams.spotDist = physical_PB_spacing / CPB_spacing`, where both terms come from explicit geometry transforms; reject runs where either spacing is non-positive or where the implied ratio drifts from the declared lattice contract.

  **Implementation Status (2026-05-09):** `expectedHaloSpotDistInRays` now derives from `layerHaloPlan->spotDelta.x / beam.raySpacing.x` (was `rawSpotLattice.spotDelta.x / beam.raySpacing.x`). Site-2 `iddParams.spotDist` computation at line ~4036 dropped its `* lenToMm` for the halo branch (companion to 9.48) so the ratio is dimensionless mm/mm. New 9.55 hard fail-fast guards against `spotDist < 1.5`.
- [x] 9.34 Mirror RTD-main `devNucSpotIdx` semantics exactly in the active halo path: map each physical PB center to CPB ray index using one deterministic projection and write one fixed halo spot index per mapped ray; do not use nearest-from-unique-axis lookup, and do not let missing weights delete lattice coordinates.

  **Implementation Status (2026-05-09):** `buildHaloLatticePlan` lines ~882-901 already implement upstream-equivalent projection (`for spotIdxY, for spotIdxX, write nucSpotIdx[ray]`). With 9.23 making the input lattice canonical, this loop now iterates over physical PB cells and writes one halo spot index per nearest CPB ray, exactly matching `kernel_wrapper.cu:901-911`.
- [ ] 9.35 Add a focused parity checklist tied to upstream reference points (`kernel_wrapper.cu` around the `nucRayDims` setup and `spotDistInRays` assignment): for one fixture, dump and compare `{physical delta/offset, raw HPB dims, padded HPB dims, spotDistInRays, mapped devNucSpotIdx count}` between local wrapper and RTD-main expectations.
- [ ] 9.36 After 9.31-9.35, rerun the halo gate matrix and require a "center-preservation with tail-increase" verdict for gate C. If center dose still drops, block further halo physics tuning and reopen geometry/lattice parity tasks first.
- [ ] 9.37 Create a "direct copy-paste feasibility matrix" before editing halo runtime: classify each upstream halo block as `copyable`, `copy-with-rename`, or `requires-adapter`. Minimum blocks: `nucRayDims/extendAndPadd`, `nucSpotIdx` construction, `spotDistInRays`, dual-output `fillIddAndSigma` call, nuclear superposition launch set, and `nucTransfDiv`.
- [ ] 9.38 Lock the variable/adaptor map required for copy-with-rename items. At minimum map upstream `{spotGridDims, primRayDims, primRayIdxToGantry, beam.getSpotIdxToGantry().getDelta/Offset, beam.getRaySpacing, beam.getWeights}` to local `{rawSpotLattice.spotGridDims, rayDims, cpbCorner/cpbResolution/fan geometry, beam.spotDelta/spotOffset, beam.raySpacing, rawSpotLattice.spotWeights}` and fail if any mapping is implicit or heuristic.
- [x] 9.39 Remove copy blockers caused by wrapper-specific indirection before halo parity edits: specifically eliminate `buildHaloLatticePlan` axis reconstruction (`uniqX/uniqY` + nearest-axis lookup) from the active path so upstream `extendAndPadd + direct index projection` can be transplanted without semantic drift.

  **Implementation Status (2026-05-09):** Closed by 9.23 — the WEQ-header-driven cardinality/spacing path is removed; halo path now consumes the canonical PB lattice view directly.
- [ ] 9.40 Define one acceptance rule for "copy complete": for each migrated block, local code must be structurally isomorphic to upstream RTD-main logic (same loop/index math and normalization meaning), with only naming/ownership adaptations documented in the feasibility matrix.
- [x] 9.41 Separate "dense WEQ departure plane" from "physical PB lattice" explicitly in the halo contract. When `spotPositionsAreIndices=true`, the local wrapper must not silently treat `weqHeader{nx,ny,dx,dy,x0,y0}` as proof that the halo HPB grid equals the full ray/WEQ texel lattice; add one validated adapter or a hard rejection so halo mode only proceeds when the exported `idbeamxy` contract is proven to encode the upstream `spotIdxToGantry` physical PB positions.
- [x] 9.42 Remove halo-side bilinear redistribution drift from `buildRawSpotLattice(...)` / nuclear-weight preparation unless it is proven upstream-equivalent. The upstream halo branch assumes one nuclear PB weight per physical PB centre; it does not split one HPB across multiple texels just because the exported CarbonPBS coordinates are sub-texel on the dense WEQ plane. If the current bilinear rasterization remains necessary for the primary CPB helper path, isolate it from the halo HPB source instead of reusing it by accident.
- [x] 9.43 Split the local meanings of `RawSpotLattice` before claiming RTD-main parity. If `RawSpotLattice` remains a dense departure-plane helper for primary convolution, add a second validated physical-PB view for halo (`delta/offset/cardinality/weights`) rather than reusing the dense WEQ-plane object as both "primary helper lattice" and "upstream HPB lattice" without proof.

  **Implementation Status (2026-05-08):**
  - Framework: Gate 9.41 now rejects `spotPositionsAreIndices=true` + nuclear_correction combo.
  - Gate 9.42 now documents the bilinear rasterization issue explicitly in `buildHaloLatticePlan`.
  - Next step: Create a `PhysicalPBLatticeView` struct when building haloPlans, using nearest-point assignment instead of bilinear rasterization for HPB-to-ray mapping.
  - Timeline: This is a med-term refactoring; current approach (with gate 9.41) blocks invalid combos while preserving valid paths.
- [x] 9.44 Restore upstream-equivalent large-sigma behavior in superposition before trusting halo tails. The current `performCompleteTileBasedSuperposition(...)` overflow clamp that buckets radii larger than `MAX_SUPERP_RADIUS` into the max-radius launch is not numerically neutral for halo; either mirror the upstream overflow semantics exactly or convert overflow into an explicit fail-fast gate instead of silently truncating wide halo kernels.

  **Implementation Status (2026-05-08):**
  - Added explicit warning in `complete_superposition.cu` identifying overflow as HIGH RISK for halo tails.
  - Marked as TODO: Implement upstream RTD-main overflow semantics or fail-fast gate.
  - Current status: Auditable warning present; full fix deferred to future numerics campaign.

  **Plan B implementation (2026-05-09):** Combined `performCompleteTileBasedSuperposition` deleted; replaced with two upstream-faithful functions in `src/algorithms/complete_superposition.cu` mirroring `kernel_wrapper.cu` (lines 985-1108):
  - `performPrimaryTileBasedSuperposition` — clamp+warn overflow policy (Carbon-class plans may exceed kMaxSuperpR; central dose preserved, tails truncated with audit log).
  - `performNuclearTileBasedSuperposition` — strict throw on overflow (matches RTD-main `kernel_wrapper.cu:1004` exactly; silent halo-tail truncation forbidden).
  - Each function locally allocates and frees its own `devTileRadCtrs` and `devInOutIdcs` scratch, so the two scratch sets are never simultaneously resident on the default stream — the design choice that "memory expenditure is reduced through separated calls" (Plan B requirement). Both accept an optional `cudaStream_t` parameter (default 0) so a future stream-aware caller (task 8.4) can issue them concurrently with truly independent buffers.
  - Wrapper call sites at `src/core/raytracedicom_wrapper.cu` ~5067 / ~5073 updated to call the two new functions; the orphaned old function body that had been left in place after the rename (lines 711-884) is removed, eliminating the `error: identifier "performCompleteTileBasedSuperposition" is undefined` compile error.
- [ ] 9.45 Lock the BEV dose texture creation contract for device-backed inputs. `create3DTexture(...)` and every fallback path it can reach must either support device pointers correctly in all cases or reject the call; a HostToDevice-only fallback path for device-resident `devBevPrimDose` / `devBevNucDose` is an unacceptable runtime divergence from the upstream BEV sampling contract.
- [ ] 9.46 Remove silent interpolation-model drift in `primTransfDiv` / `nucTransfDiv`. The local linear-memory trilinear fallback is only acceptable if it is shown numerically equivalent to the texture-object path for the same BEV buffer, coordinates, and border rules; otherwise texture creation failure must abort transfer instead of silently changing the BEV sampling model for primary or halo.
- [ ] 9.47 Make texture-array ownership explicit for correctness, not just performance. The wrapper currently destroys texture objects but does not explicitly release the backing arrays created inside helper wrappers; repeated TPS calls must not be allowed to drift away from upstream behavior by leaking arrays, forcing late allocation failures, or accidentally switching later runs onto different texture/fallback paths.

### 9.48-9.55 Halo energy-conservation staging (added 2026-05-08)

Sequencing note (REQUIRED — read before touching halo runtime again):
The halo dose-deficit root-cause analysis (`openspec/changes/extend-wrapper-and-pybind-operator-family/halo-energy-conservation.md`) established that the dominant mechanism for "halo-on dose < primary-only" is RC2 (BEV-positioning bug at `raytracedicom_wrapper.cu:5132-5141`), NOT lattice sparsity (RC1) on its own. RC1/RC3/RC4 are required for upstream-equivalent lateral profile but do not move the integrated dose unless RC2 is fixed. Therefore tasks 9.21-9.40 are blocked behind 9.48; do not start 9.23 or 9.34 implementation until 9.48 audit gate is green.

- [x] 9.48 Fix the redundant `lenToMm` doubling in `nucIdxToFan` (`src/core/raytracedicom_wrapper.cu` ~5132-5141). Upstream `kernel_wrapper.cu:1240` constructs `Float3FromFanTransform nucRayIdxToDoseIdx(beam.getSpotIdxToGantry(), …)` with delta/offset already in mm (no extra unit conversion). Local `layerHaloPlan->spotDelta` and `spotOffset` are sourced from `weqHeader[7]/[4]/[6]/[3]` which are already in mm, so multiplying by `lenToMm=10` again places the halo BEV origin ~10× outside the dose volume and `nucTransfDiv` returns 0 at every voxel. The fix is to drop both `* lenToMm` factors and confirm `fanDelta_mm.z` / `fanCorner_mm.z` already match upstream conventions on the z-axis. This single fix is expected to restore integrated halo dose to within a few percent of upstream even if RC1/RC3/RC4 remain.

  **Implementation Status (2026-05-09):** Both `* lenToMm` factors dropped at line 5132-5139. Companion site at line 4034-4035 (`spotSpacingMm = layerHaloPlan->spotDelta.x * lenToMm`) also fixed for the halo branch only. Non-halo branches (cm-sourced `rawSpotLattice` / `beam.spotDelta`) keep their explicit `* lenToMm` conversion. Verified with `NUCLEAR_CORR=GAUSS_FIT` build.
- [x] 9.49 Add an energy-conservation audit print to `wrapper_integration_test` (or to `[RTD_HALO_AUDIT]` log surface) that captures per-layer:
  - `E_subtracted = nucWeight × Σ_rays rayWeight × Δstep_dE_proxy`
  - `E_added = nucWeight × Σ_nucIdx nucRayWeight × Δstep_dE_proxy / spotDist²`
  - their ratio (must be 1 ± 1% in upstream-equivalent setup; will be 1 ± 5% even with RC1/RC3/RC4 unfixed once RC2 lands)
  This audit catches RC2-class regressions but is INSENSITIVE to RC1/RC3/RC4 lateral-profile issues; treat it as a necessary-but-not-sufficient gate. Do not interpret a green ratio as parity.

  **Implementation Status (2026-05-09):** `[RTD_HALO_ENERGY]` per-layer log added at the post-`fillIddAndSigma` synchronization point. Prints `sum_rayWeight`, `sum_nucRayWeight`, `spotDistSq`, and the dimensionless `nuc/ray_should_be_1` ratio. Gated on `runtimeNuclearEnabled && fineTiming`. Note: documented in halo-energy-conservation.md section 6 as RC3/RC4-sensitive but RC2-insensitive.
- [ ] 9.50 Add a halo lateral-profile audit gate that complements 9.49 (sufficient gate for RC1/RC3/RC4). For one fixture (`bg838_beam964`), dump the halo BEV after `fillIddAndSigma` and verify the support occupancy: upstream-equivalent halo has `nucRayDims ≈ padded(spotGridDims)` with dense weight and a coarse-grid σ-stretched Gaussian on top; the current local has `nucRayDims ≈ CPB-grid` with sparse weight and a primary-grid σ. The gate compares occupancy fraction (`nonzero_cells / total_cells` of `bevNucIdd[layer]`) and effective lateral spread (FWHM of summed-over-z BEV halo) against the upstream-faithful predictions.

  **Partial Implementation Status (2026-05-09):** `[RTD_HALO_AUDIT]` extended with `nucRayDims`, `paddedNonzero`, `paddedTotal`, `occupancy`. Pre-9.23: occupancy ~0.4% on padded 96×64 dense WEQ grid. Post-9.23: occupancy ~2.4% on padded 32×32 PB grid. Numerical FWHM comparison vs upstream still requires CUDA-runtime fixture (deferred — `cudaErrors driver insufficient` blocker).
- [ ] 9.51 After 9.48 lands and 9.49 confirms integrated balance, freeze the gate-A/gate-B regression baseline for proton-only and `NUCLEAR_CORR` build with `nuclear_correction=false` runs. They must be byte-identical (or within float32 round-off) to the pre-9.48 baseline; the RC2 fix only touches the halo branch, so primary and proton-only paths must be unaffected. This protects 9.21-9.40 implementation from accidentally perturbing the proton path.
- [ ] 9.52 Re-sequence 9.21-9.40 implementation around the canonical lattice transform. The minimal-diff order is:
  1. 9.39 (remove WEQ-grid axis reconstruction blocker in `buildPhysicalPBLatticeView`)
  2. 9.23 (rebuild `HaloLatticePlan` from canonical PB transform: infer `nxRef/nyRef/dxRef/dyRef/oxRef/oyRef` from decoded spot centers, NOT from `weqHeader[8]/[5]/[7]/[4]/[6]/[3]`)
  3. 9.31, 9.32, 9.34 (lattice source-of-truth, `extendAndPadd` semantics, `devNucSpotIdx` projection)
  4. 9.33, 9.24 (`spotDistInRays = physical_PB_spacing / CPB_spacing`; this becomes automatic after step 2 since `layerHaloPlan->spotDelta.x` will be the PB spacing)
  Each sub-step must keep 9.49 green and not regress 9.51 baselines.

  **Implementation Status (2026-05-09):** Steps 1-4 all landed in one coherent commit (9.23 + 9.31 + 9.32 + 9.34 + 9.33 + 9.24 + 9.39). Static build verified. Runtime gate-A/B/C still pending CUDA fixture.
- [x] 9.53 Document the `lenToMm` audit explicitly in code: at every site where layer/halo plan deltas/offsets are mixed with fan-frame mm-units, add a one-line comment stating the unit of each operand. The current bug exists because `layerHaloPlan->spotDelta` (mm) was assumed to share units with `beam.spotDelta` (cm) without the wrapper layer asserting either contract. Adding asserts that `layerHaloPlan->spotDelta.x > 0.0f && layerHaloPlan->spotDelta.x < 100.0f` (typical PB spacing in mm) catches the regression cheaply.

  **Implementation Status (2026-05-09):** Unit-contract comment block added above `buildHaloLatticePlan` and `buildPhysicalPBLatticeView`. Runtime guard added before `nucIdxToFan` construction: throws if `spotDelta` is outside (0, 100) mm range. Companion `* lenToMm` site at line 4034 also annotated.
- [ ] 9.54 After 9.48-9.52 are green on the controlled fixture, re-evaluate 9.21 (lock physical PB lattice contract) wording: it currently says "primary CPB rays may be dense, but halo HPB must be derived from one stable physical PB grid per layer". Confirm whether per-layer physical PB grids actually differ (in CarbonPBS they typically do not) and whether one beam-global PB grid is enough; if so, simplify the contract.
- [x] 9.55 Hard fail-fast gate: when `runtimeNuclearEnabled` is true and `iddParams.spotDist < 1.5f`, abort the beam with a precise error referencing the user's RC4 framing. This catches future regressions where someone "fixes" `spotDistInRays = 1.0f` again because the WEQ-grid path silently makes it look correct. Threshold 1.5 is chosen because any plausible PB/CPB ratio is ≥ 2 in practice; 1.0 is mathematically valid only if PB spacing = CPB spacing, which is not the upstream HPB regime.

  **Implementation Status (2026-05-09):** Added immediately after `iddParams.spotDist = fabsf(spotSpacingMm / fanDelta_mm.x)` at line ~4036. Throws `runtime_error` with the actual spotDist value when `runtimeNuclearEnabled && spotDist < 1.5`. Pre-9.23 this fired on every halo run (intended); post-9.23 it only fires on canonical-inference regressions.

  **Relaxation pass (2026-05-09):** Demoted from `throw` to `[9.55] WARN` (warn-once-per-thread × 3) so halo can run in WEQ-header fallback mode (where spotDist collapses to ~1). The warning still flags the lateral-profile regression for visibility. RC2 BEV fix continues to apply, restoring integrated halo dose to ~correct even when this warning fires.

### 9.56-9.59 spotDist root-cause fix: mirror upstream kernel_wrapper.cu:941 (added 2026-05-09)

Background: upstream RTD-main uses `beam.getSpotIdxToGantry().getDelta().x / beam.getRaySpacing().x` as a simple direct lookup with no inference and no fallback. Nuclear σ_eff is compressed by `1/spotDist` in ray-space; with canonical spotDist ≥ 2 the tile radius stays within `maxSuperpR=32`. The local code reaches the [9.44] throw because `buildPhysicalPBLatticeView()` falls back to `weqHeader` when canonical PB inference fails, returning `spotDelta = CPB spacing → spotDist = 1 → σ_eff uncompressed → radius >> 32`.

Sequencing: 9.56 and 9.57 are the short-term unblock (make spotDist correct at source). 9.58 makes the fallback path explicit. 9.59 is the pre-compute gate so overflow at [9.44] becomes impossible for valid inputs.

- [x] 9.56 Fix the `cuFinalDose` pybind path that erroneously sets `beam.spotDelta = weqHeader[7/4]` (CPB spacing). In `src/bindings/raytracedicom_pybind.cpp` around line 1199: add an optional `spot_delta` argument to `cuFinalDose` (or derive it from the beam dict if already present), set `beamSettings.spotDelta` to the physical PB spacing when provided, and leave it zero otherwise. Add a runtime WARN when `nuclear_correction=true` but `beamSettings.spotDelta.x ≤ beamSettings.raySpacing.x` so the caller knows the nuclear path will degrade. This is the pybind-side mirror of the upstream contract where `beam.getSpotIdxToGantry().getDelta()` is always the physical PB lattice.

- [x] 9.57 Mirror upstream `spotDistInRays` source priority in `src/core/raytracedicom_wrapper.cu`. Upstream (line 941): `spotDistInRays = beam.getSpotIdxToGantry().getDelta().x / beam.getRaySpacing().x` — direct, no inference. Current local (line 4058): prefers `layerHaloPlan->spotDelta` (inferred or fallback, can equal CPB spacing). Fix: when `beam.spotDelta.x > beam.raySpacing.x` (i.e., an explicit and valid physical PB spacing was provided by the caller), use `beam.spotDelta.x / beam.raySpacing.x` as `spotDistInRays`, bypassing the inference path entirely. The inference path (`layerHaloPlan->spotDelta`) should only be used when `beam.spotDelta.x == 0` (field not set by caller). This single priority swap eliminates the [9.44] / [9.55] failure pair for callers that supply a correct `spot_delta`.

- [x] 9.58 Make `buildPhysicalPBLatticeView` distinguish canonical PB inference success from WEQ-header fallback. Currently the function returns `true` for both outcomes, hiding the degraded path from callers. Add a `bool usedFallback` field to `RawSpotLattice` (false = canonical inferred, true = weqHeader fallback). In the halo lattice setup block (`src/core/raytracedicom_wrapper.cu` ~3363): if `physicalPbLattice.usedFallback == true` AND `runtimeNuclearEnabled` AND `beam.spotDelta.x == 0` (i.e., no explicit override from 9.57), throw a descriptive error instead of silently proceeding with wrong spotDist. This converts the silent-fail-later pattern into a clear fail-fast-before-allocation at the lattice-build site.

- [x] 9.59 Add a pre-allocation gate for the nuclear superposition path: compute and validate `spotDistInRays` before allocating `devNucIdd`, `devNucRSigmaEff`, or `devNucSpotIdx`. The current ordering is: allocate → fillIddAndSigma → tileRadCalc → [9.44] throw (28000 tiles computed then discarded). The corrected ordering should be: compute spotDistInRays → gate check (< 1.5 → throw with beam.spotDelta / beam.raySpacing diagnostic) → allocate nuclear buffers → fillIddAndSigma → tileRadCalc. This ensures the [9.44] throw in `performNuclearTileBasedSuperposition` is only reachable when the nuclear LUT depth axis itself is unexpectedly wide (a genuine physics configuration issue), not when spotDist was simply computed wrong. Cross-reference: the [9.55] WARN that currently fires at post-fillIddAndSigma should be removed once this pre-gate is active.

